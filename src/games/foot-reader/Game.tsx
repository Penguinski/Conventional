import LocalImageReader from "../../components/LocalImageReader";
import type { GameProps } from "../types";
export default function FootReader(props: GameProps) { return <LocalImageReader kind="foot" {...props} />; }
