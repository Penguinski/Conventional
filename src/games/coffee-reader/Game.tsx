import LocalImageReader from "../../components/LocalImageReader";
import type { GameProps } from "../types";
export default function CoffeeReader(props: GameProps) { return <LocalImageReader kind="coffee" {...props} />; }
